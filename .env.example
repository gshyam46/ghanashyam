# Optional: override the production domain supplied automatically by Vercel.
# Local builds default to https://ghanashyamg.vercel.app when no override is set.
# NEXT_PUBLIC_SITE_URL=https://your-domain.example
